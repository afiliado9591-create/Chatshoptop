/* ShopAds Admin — Etapa 9: revisão, saldos, saques e botão admin robusto. */
(function(){'use strict';
const ADMIN_EMAILS=['jeanaguiar636@gmail.com','afiliado9591@gmail.com'],$=(s,r)=>(r||document).querySelector(s);
function dbRef(){try{return typeof db!=='undefined'&&db?db:null}catch(e){return null}}
function authUser(){try{if(typeof currentUser!=='undefined'&&currentUser)return currentUser}catch(e){}try{if(typeof firebase!=='undefined'&&firebase.auth&&firebase.auth().currentUser)return firebase.auth().currentUser}catch(e){}return null}
function currentEmail(){const u=authUser();return String(u&&u.email||'').trim().toLowerCase()}
function adminOk(){try{if(typeof isAdmin!=='undefined'&&isAdmin===true)return true}catch(e){}return ADMIN_EMAILS.includes(currentEmail())}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function brl(v){return'R$ '+Number(v||0).toFixed(2).replace('.',',')}
function when(v){if(!v)return'—';try{return new Date(v).toLocaleString('pt-BR')}catch(e){return String(v)}}
function ensureStyles(){if($('#shopAdsAdminStyles'))return;const s=document.createElement('style');s.id='shopAdsAdminStyles';s.textContent=`.sa-admin-btn{margin-top:12px;background:#111827!important;color:#fff!important}.sa-admin-panel{position:fixed;inset:0;z-index:10070;background:#f3f4f6;overflow:auto;font-family:Arial,sans-serif;color:#111827}.saa-top{background:#111827;color:#fff;padding:16px}.saa-head{max-width:920px;margin:auto;display:flex;justify-content:space-between;align-items:center}.saa-close{border:0;background:#ffffff22;color:#fff;border-radius:50%;width:40px;height:40px}.saa-body{max-width:920px;margin:auto;padding:16px}.saa-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.saa-metric,.saa-card{background:#fff;border:1px solid #e5e7eb;border-radius:14px;padding:14px;margin-bottom:14px}.saa-metric small{display:block;color:#6b7280;font-weight:700}.saa-metric b{font-size:24px}.saa-row{border-top:1px solid #eee;padding:12px 0}.saa-title{font-weight:900}.saa-meta{font-size:12px;color:#6b7280;line-height:1.5}.saa-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}.saa-actions button,.saa-ok{border:0;border-radius:9px;padding:9px 11px;font-weight:800}.saa-ok{background:#dcfce7;color:#166534}.saa-block{background:#fee2e2;color:#991b1b}.saa-muted{background:#f3f4f6;color:#374151}.saa-badge{display:inline-block;border-radius:999px;padding:4px 7px;font-size:10px;font-weight:900;background:#e5e7eb}.saa-badge.live,.saa-badge.approved{background:#dcfce7;color:#166534}.saa-badge.pending,.saa-badge.review{background:#fef3c7;color:#92400e}.saa-badge.blocked,.saa-badge.refused{background:#fee2e2;color:#991b1b}.saa-alert{background:#fff7ed;color:#9a3412;border:1px solid #fed7aa;border-radius:10px;padding:10px;font-size:12px;margin:12px 0}.saa-summary{display:flex;gap:10px;flex-wrap:wrap}.saa-summary span{padding:8px;background:#f9fafb;border-radius:8px}@media(max-width:700px){.saa-grid{grid-template-columns:repeat(2,1fr)}}`;document.head.appendChild(s)}
async function getCollection(n){const d=dbRef();if(!d)throw Error('Firebase indisponível');const s=await d.collection(n).get();return s.docs.map(x=>({id:x.id,...x.data()}))}
async function setCampaignStatus(id,status){await dbRef().collection('shopadsCampaigns').doc(id).set({status,updatedAt:new Date().toISOString(),moderatedAt:new Date().toISOString(),moderatedBy:currentEmail()},{merge:true})}
async function setClickBalanceStatus(id,status){await dbRef().collection('shopadsClicks').doc(id).set({paymentStatus:status,balanceStatus:status,balanceReviewedAt:new Date().toISOString(),balanceReviewedBy:currentEmail()},{merge:true})}
async function setWithdrawalStatus(id,status){await dbRef().collection('shopadsWithdrawals').doc(id).set({status,updatedAt:new Date().toISOString(),reviewedAt:new Date().toISOString(),reviewedBy:currentEmail()},{merge:true})}
async function approvePromoterPending(p,clicks){const a=clicks.filter(x=>x.promoterUid===p&&x.valid!==false&&!x.duplicate&&(x.paymentStatus||x.balanceStatus||'pendente')!=='aprovado');for(const x of a)await setClickBalanceStatus(x.id,'aprovado')}
function statusLabel(v){if(v==='teste')return['PUBLICADA','live'];if(v==='revisao')return['AGUARDANDO APROVAÇÃO','review'];if(v==='bloqueada')return['BLOQUEADA','blocked'];return['RASCUNHO','']}
async function openAdmin(){if(!adminOk())return alert('Área exclusiva do administrador.');ensureStyles();let el=$('#shopAdsAdminPanel');if(!el){el=document.createElement('div');el.id='shopAdsAdminPanel';el.className='sa-admin-panel';document.body.appendChild(el)}el.style.display='block';el.innerHTML=`<div class="saa-top"><div class="saa-head"><h1>🛡️ Admin ShopAds</h1><button class="saa-close">×</button></div></div><main class="saa-body"><div id="saaContent">Carregando...</div></main>`;$('.saa-close',el).onclick=()=>el.style.display='none';const box=$('#saaContent',el);try{const[campaigns,clicks,links,withdrawals]=await Promise.all([getCollection('shopadsCampaigns'),getCollection('shopadsClicks'),getCollection('shopadsLinks'),getCollection('shopadsWithdrawals').catch(()=>[])]),valid=clicks.filter(x=>x.valid!==false&&!x.duplicate),approvedTotal=valid.filter(x=>(x.paymentStatus||x.balanceStatus)==='aprovado').reduce((s,x)=>s+Number(x.reward||0),0),pendingTotal=valid.reduce((s,x)=>s+Number(x.reward||0),0)-approvedTotal,reviews=campaigns.filter(c=>c.status==='revisao').length,withdrawPending=withdrawals.filter(x=>x.status==='pendente');box.innerHTML=`<div class="saa-grid"><div class="saa-metric"><small>Campanhas em revisão</small><b>${reviews}</b></div><div class="saa-metric"><small>Saldo pendente</small><b>${brl(pendingTotal)}</b></div><div class="saa-metric"><small>Saldo aprovado</small><b>${brl(approvedTotal)}</b></div><div class="saa-metric"><small>Saques aguardando</small><b>${withdrawPending.length}</b></div></div><div class="saa-alert">Modo de teste. Nenhum pagamento real é executado.</div><section class="saa-card"><h2>📣 Campanhas para aprovação</h2><div id="saaCampaigns"></div></section><section class="saa-card"><h2>🏧 Solicitações de saque</h2><div id="saaWithdrawals"></div></section><section class="saa-card"><h2>💰 Aprovação de saldo</h2><div id="saaBalances"></div></section>`;const cbox=$('#saaCampaigns',box),sorted=[...campaigns].sort((a,b)=>(a.status==='revisao'?-1:1)-(b.status==='revisao'?-1:1));cbox.innerHTML=sorted.length?sorted.map(c=>{const[l,k]=statusLabel(c.status);return`<div class="saa-row"><div class="saa-title">${esc(c.title||'Campanha')} <span class="saa-badge ${k}">${l}</span></div><div class="saa-meta">Anunciante: ${esc(c.ownerEmail||c.ownerUid||'—')}<br>${brl(c.budget)} orçamento · ${brl(c.reward)} por ${esc(c.resultType||'resultado')}<br>${esc(c.url||'')}</div><div class="saa-actions">${c.status!=='teste'?`<button class="saa-ok" data-ca="${esc(c.id)}">Aprovar e publicar</button>`:''}<button class="saa-block" data-cb="${esc(c.id)}">Bloquear</button><button class="saa-muted" data-cr="${esc(c.id)}">Voltar a rascunho</button></div></div>`}).join(''):'<p>Nenhuma campanha.</p>';cbox.onclick=async e=>{const a=e.target.closest('[data-ca]'),b=e.target.closest('[data-cb]'),r=e.target.closest('[data-cr]');try{if(a)await setCampaignStatus(a.dataset.ca,'teste');if(b)await setCampaignStatus(b.dataset.cb,'bloqueada');if(r)await setCampaignStatus(r.dataset.cr,'rascunho');if(a||b||r)openAdmin()}catch(err){alert(err.message)}};const wbox=$('#saaWithdrawals',box);wbox.innerHTML=withdrawals.length?withdrawals.sort((a,b)=>String(b.createdAt||'').localeCompare(String(a.createdAt||''))).map(w=>`<div class="saa-row"><div class="saa-title">${esc(w.promoterEmail||w.promoterUid)} <span class="saa-badge ${w.status==='aprovado'?'approved':w.status==='recusado'?'refused':'pending'}">${String(w.status||'pendente').toUpperCase()}</span></div><div class="saa-meta">${brl(w.amount)} · ${when(w.createdAt)}</div>${w.status==='pendente'?`<div class="saa-actions"><button class="saa-ok" data-wo="${esc(w.id)}">Aprovar saque</button><button class="saa-block" data-wn="${esc(w.id)}">Recusar</button></div>`:''}</div>`).join(''):'<p>Nenhuma solicitação.</p>';wbox.onclick=async e=>{const a=e.target.closest('[data-wo]'),n=e.target.closest('[data-wn]');if(!a&&!n)return;try{await setWithdrawalStatus(a?a.dataset.wo:n.dataset.wn,a?'aprovado':'recusado');openAdmin()}catch(err){alert(err.message)}};const grouped={};valid.forEach(x=>{const p=x.promoterUid||'sem-divulgador';if(!grouped[p])grouped[p]={uid:p,email:x.promoterEmail||'',pending:0,approved:0,count:0};const v=Number(x.reward||0);if((x.paymentStatus||x.balanceStatus)==='aprovado')grouped[p].approved+=v;else{grouped[p].pending+=v;grouped[p].count++}});const bbox=$('#saaBalances',box),groups=Object.values(grouped);bbox.innerHTML=groups.length?groups.map(g=>`<div class="saa-row"><div class="saa-title">${esc(g.email||g.uid)}</div><div class="saa-summary"><span>Pendente: <b>${brl(g.pending)}</b></span><span>Aprovado: <b>${brl(g.approved)}</b></span></div>${g.count?`<div class="saa-actions"><button class="saa-ok" data-ap="${esc(g.uid)}">Aprovar saldo pendente</button></div>`:''}</div>`).join(''):'<p>Nenhum saldo.</p>';bbox.onclick=async e=>{const b=e.target.closest('[data-ap]');if(!b)return;try{await approvePromoterPending(b.dataset.ap,clicks);openAdmin()}catch(err){alert(err.message)}}}catch(err){box.innerHTML=`<div class="saa-alert">Não foi possível carregar: ${esc(err.message)}</div>`}}
function injectAdminButton(){const o=$('#shopAdsOverlay');if(!o||!o.classList.contains('open'))return;const old=$('#shopAdsAdminBtn',o);if(!adminOk()){if(old)old.remove();return}if(old)return;const r=o.querySelector('[data-results]');const card=r&&r.closest('.sa-card');if(!card)return;const b=document.createElement('button');b.id='shopAdsAdminBtn';b.type='button';b.className='sa-btn sa-admin-btn';b.textContent='🛡️ Admin ShopAds';b.onclick=openAdmin;card.appendChild(b)}
ensureStyles();window.openShopAdsAdmin=openAdmin;new MutationObserver(injectAdminButton).observe(document.documentElement,{childList:true,subtree:true,attributes:true,attributeFilter:['class']});setInterval(injectAdminButton,500);try{if(typeof firebase!=='undefined'&&firebase.auth)firebase.auth().onAuthStateChanged(()=>setTimeout(injectAdminButton,50))}catch(e){}injectAdminButton();
})();